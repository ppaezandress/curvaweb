node_modules/
dist/
.astro/
.DS_Store

# Backups/zips locales — NO commitear (gotcha app.zip 2.6GB)
*.zip
app.zip

# Playwright (artefactos de tests e2e)
test-results/
playwright-report/
.gstack/

# impeccable: estado de tooling / snapshots de critique (no es del sitio)
.impeccable/
