# Webhook (n8n) que recibe los correos del lead magnet "checklist 10 señales".
# Debe aceptar POST JSON: { email, source, ts }.
# Copia este archivo a `.env` y pon la URL real de tu webhook de n8n.
# TODO Tier 1: pendiente de coordinar la URL real con Andrés.
PUBLIC_LEADMAGNET_WEBHOOK=
