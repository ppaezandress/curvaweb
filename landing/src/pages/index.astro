---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import Curve from '../components/Curve.astro';
import { areas } from '../data/areas';
import { casos } from '../data/casos';

const calLink = 'https://cal.com/andres-paez/30min';
const waLink = 'https://wa.me/5215611846200?text=Hola%2C%20me%20interesa%20saber%20m%C3%A1s%20sobre%20CURVA';

const accentText = { ember: 'text-ember', gold: 'text-gold', jade: 'text-jade' } as const;
const accentBar = { ember: 'bg-ember', gold: 'bg-gold', jade: 'bg-jade' } as const;
const accentSoft = { ember: 'bg-ember/12', gold: 'bg-gold/15', jade: 'bg-jade/12' } as const;

const icons: Record<string, string> = {
  compass: 'M12 21a9 9 0 100-18 9 9 0 000 18zm3.5-12.5l-2 5.5-5.5 2 2-5.5 5.5-2z',
  bolt: 'M13 2L4.5 13.5H11l-1 8.5L19.5 10H13l0-8z',
  spark: 'M12 2l2.1 6.3L20 10l-5.9 1.7L12 18l-2.1-6.3L4 10l5.9-1.7L12 2z',
};

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  name: 'CURVA',
  description: 'Consultoría de transformación operativa y digital para empresas en crecimiento.',
  areaServed: 'México',
  address: { '@type': 'PostalAddress', addressLocality: 'Ciudad de México', addressCountry: 'MX' },
  serviceType: ['Consultoría empresarial', 'Digitalización', 'Marketing'],
  founder: { '@type': 'Person', name: 'Andrés Páez Cortazar' },
};

const problema = [
  'El fundador carga todo y el negocio no funciona sin él.',
  'Los procesos viven en la cabeza de ciertas personas.',
  'Se vende más, pero el desorden crece igual.',
  'Hay voluntad de crecer, pero falta estructura.',
];
const proceso = [
  { num: '01', title: 'Diagnóstico', desc: 'Entendemos la verdad del negocio.' },
  { num: '02', title: 'Ruta por módulos', desc: 'Primero lo urgente, luego lo estructural.' },
  { num: '03', title: 'Implementación', desc: 'Configuramos, diseñamos, capacitamos.' },
  { num: '04', title: 'Acompañamiento', desc: 'Hasta que el cambio se sostenga solo.' },
];
const marqueeItems = ['Diagnóstico','Digitalización','ERPs','Procesos','Gobernanza','Cultura','Liderazgo','Automatización'];
---

<BaseLayout
  title="CURVA. Transformación operativa y digital"
  description="Ordenamos, digitalizamos y potenciamos la operación de empresas en crecimiento para que sus fundadores recuperen el control y escalen con claridad."
  jsonLd={jsonLd}
>
  <Nav overHero calLink={calLink} />

  <a href="#problema" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:bg-ember focus:text-white focus:px-4 focus:py-2 focus:rounded-full focus:text-sm focus:font-semibold">Saltar al contenido</a>

  <!-- ============================== HERO ============================== -->
  <section class="surface-ink text-sand-50 relative min-h-[92vh] flex flex-col justify-center overflow-hidden">
    <Curve variant="hero" draw="auto" stroke={1.6} class="pointer-events-none absolute inset-x-0 bottom-0 w-full h-[26vh] opacity-40" />
    <div class="mx-auto max-w-6xl w-full px-6 sm:px-10 relative z-10 pt-24">
      <p class="eyebrow text-ember-300 mb-7">Transformación operativa y digital</p>
      <h1 class="display text-[2.6rem] leading-[1.06] sm:text-5xl lg:text-[3.6rem] font-semibold max-w-3xl tracking-tight">
        Que tu empresa funcione por <span class="curve-text">estructura</span>, no por esfuerzo.
      </h1>
      <p class="mt-7 text-lg sm:text-xl text-sand-300 leading-relaxed max-w-xl">
        Ordenamos, digitalizamos y potenciamos tu operación para que recuperes el control y escales con claridad.
      </p>
      <div class="mt-9 flex flex-col sm:flex-row sm:items-center gap-x-6 gap-y-4">
        <a href={calLink} target="_blank" rel="noopener" class="group inline-flex w-fit items-center justify-center gap-2 text-base font-semibold bg-ember text-white px-7 py-4 rounded-full hover:bg-ember-600 transition-all hover:gap-3">
          Agendar diagnóstico gratuito
          <svg class="w-4 h-4 transition-transform group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path></svg>
        </a>
        <a href="#servicios" class="text-base font-medium text-sand-300 hover:text-sand-50 transition-colors">Ver qué hacemos →</a>
      </div>
    </div>
    <a href="#problema" aria-label="Bajar" class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce"><svg class="w-6 h-6 text-sand-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 13.5L12 21m0 0l-7.5-7.5M12 21V3"></path></svg></a>
  </section>

  <!-- ============================== MARQUEE ============================== -->
  <div class="py-4 bg-ink text-sand-50 select-none overflow-hidden whitespace-nowrap border-y border-white/5">
    <div class="marquee-track inline-block text-[12px] font-medium tracking-[0.2em] uppercase text-sand-500">
      {Array.from({ length: 2 }).map(() => (
        <span>{marqueeItems.map((m) => (<Fragment><span class="mx-5">{m}</span><span class="text-ember/50">/</span></Fragment>))}</span>
      ))}
    </div>
  </div>

  <!-- ============================== EL PROBLEMA ============================== -->
  <section id="problema" class="py-24 sm:py-36 px-6 sm:px-10 bg-sand-50">
    <div class="mx-auto max-w-7xl grid lg:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)] gap-12 lg:gap-24">
      <div class="lg:sticky lg:top-28 self-start">
        <p class="eyebrow text-ember mb-5">Para quién es esto</p>
        <h2 class="display text-3xl sm:text-4xl lg:text-5xl font-semibold leading-[1.08] tracking-tight text-ink">Ya operas y vendes. Pero la estructura no aguanta.</h2>
      </div>
      <ul class="border-t border-sand-200 self-center">
        {problema.map((item, i) => (
          <li class="flex gap-6 items-baseline py-7 border-b border-sand-200">
            <span class={`display text-base font-semibold tabular-nums ${accentText[areas[i % 3].accent]}`}>0{i + 1}</span>
            <p class="text-xl sm:text-2xl text-ink leading-snug">{item}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ============================== SERVICIOS ============================== -->
  <section id="servicios" class="py-24 sm:py-36 px-6 sm:px-10 bg-white border-t border-sand-200">
    <div class="mx-auto max-w-7xl">
      <div class="max-w-2xl mb-16">
        <p class="reveal eyebrow text-ember mb-5">Qué hacemos</p>
        <h2 class="reveal display text-3xl sm:text-4xl lg:text-5xl font-semibold tracking-tight leading-[1.08] text-ink">Estrategia, tecnología y personas. A tu medida.</h2>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {areas.map((a, i) => (
          <a href={a.href} data-tilt="3" class={`reveal reveal-d${i + 1} group relative rounded-3xl bg-sand-50 border border-sand-200 p-9 flex flex-col min-h-[280px] overflow-hidden hover:shadow-2xl hover:border-sand-300 transition-all`}>
            <span class={`absolute inset-x-0 top-0 h-1 ${accentBar[a.accent]}`}></span>
            <div class={`w-14 h-14 rounded-2xl ${accentSoft[a.accent]} flex items-center justify-center mb-7`}>
              <svg class={`w-7 h-7 ${accentText[a.accent]}`} fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.6"><path stroke-linecap="round" stroke-linejoin="round" d={icons[a.icon]}></path></svg>
            </div>
            <h3 class="display text-2xl font-semibold mb-3 text-ink">{a.title}</h3>
            <p class="text-sand-600 text-[15px] leading-relaxed">{a.desc}</p>
            <span class={`mt-auto pt-7 inline-flex items-center gap-2 text-sm font-semibold ${accentText[a.accent]} group-hover:gap-3 transition-all`}>
              Ver más
              <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path></svg>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================== CÓMO TRABAJAMOS ============================== -->
  <section class="surface-ink text-sand-50 py-24 sm:py-36 px-6 sm:px-10">
    <div class="mx-auto max-w-7xl">
      <h2 class="reveal display text-3xl sm:text-4xl lg:text-5xl font-semibold tracking-tight leading-[1.08] mb-16 max-w-2xl">No entregamos reportes. <span class="text-sand-500">Construimos el cambio.</span></h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-x-8 gap-y-12">
        {proceso.map((s, i) => (
          <div class={`reveal reveal-d${i + 1}`}>
            <p class="display curve-text text-5xl font-semibold mb-5">{s.num}</p>
            <div class="border-t border-white/15 pt-5">
              <h3 class="display font-semibold text-lg mb-2">{s.title}</h3>
              <p class="text-sand-400 text-[15px] leading-relaxed">{s.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ============================== CASOS ============================== -->
  <section id="casos" class="py-24 sm:py-36 px-6 sm:px-10 bg-sand-50">
    <div class="mx-auto max-w-7xl">
      <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between mb-14 gap-6">
        <h2 class="reveal display text-3xl sm:text-4xl lg:text-5xl font-semibold tracking-tight leading-[1.08] text-ink max-w-xl">Proyectos que hablan por sí solos.</h2>
        <div class="flex gap-2">
          <button id="prev-case" aria-label="Anterior" class="w-12 h-12 rounded-full border border-sand-300 flex items-center justify-center hover:border-ink hover:bg-white transition-colors"><svg class="w-4 h-4 text-sand-700" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5"></path></svg></button>
          <button id="next-case" aria-label="Siguiente" class="w-12 h-12 rounded-full border border-sand-300 flex items-center justify-center hover:border-ink hover:bg-white transition-colors"><svg class="w-4 h-4 text-sand-700" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5"></path></svg></button>
        </div>
      </div>

      <div id="cases-track" class="cases-track pb-4">
        {casos.map((c) => (
          <a href={c.href} class="case-card group">
            <div class="rounded-3xl border border-sand-200 bg-white p-8 h-full flex flex-col hover:shadow-2xl hover:border-sand-300 transition-all">
              <span class={`self-start px-3 py-1 rounded-full ${accentSoft[c.accent]} ${accentText[c.accent]} text-[11px] font-semibold uppercase tracking-wide mb-6`}>{c.categoria}</span>
              <h3 class="display text-xl font-semibold mb-6 text-ink">{c.title}</h3>
              <div class="grid grid-cols-2 gap-5 mt-auto">
                {c.metrics.map((m) => (<div><p class="display text-3xl font-semibold text-ink">{m.value}</p><p class="text-xs text-sand-500 mt-1">{m.label}</p></div>))}
              </div>
            </div>
          </a>
        ))}
      </div>
      <div class="flex justify-center gap-2 mt-8">
        {casos.map((_, i) => (<button class={`case-dot w-2 h-2 rounded-full bg-sand-300 transition-colors ${i === 0 ? 'dot-active' : ''}`} data-index={i} aria-label={`Caso ${i + 1}`}></button>))}
      </div>
    </div>
  </section>

  <!-- ============================== CTA ============================== -->
  <section class="surface-ink text-sand-50 py-28 sm:py-36 px-6 sm:px-10">
    <div class="reveal mx-auto max-w-3xl text-center">
      <h2 class="display text-4xl sm:text-5xl lg:text-6xl font-semibold tracking-tight leading-[1.04]">Hablemos de tu empresa.</h2>
      <p class="mt-6 text-lg text-sand-300 max-w-md mx-auto leading-relaxed">30 minutos. Sin compromiso. Te decimos con honestidad si podemos ayudarte.</p>
      <a href={calLink} target="_blank" rel="noopener" class="group mt-9 inline-flex items-center gap-2 text-base font-semibold bg-ember text-white px-9 py-4 rounded-full hover:bg-ember-600 transition-all hover:gap-3">
        Agendar conversación
        <svg class="w-4 h-4 transition-transform group-hover:translate-x-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"></path></svg>
      </a>
    </div>
  </section>

  <a href={waLink} target="_blank" rel="noopener" class="fixed bottom-6 right-6 z-50 w-14 h-14 bg-[#25D366] rounded-full flex items-center justify-center shadow-lg hover:scale-105 transition-all" aria-label="Contactar por WhatsApp">
    <svg class="w-7 h-7 text-white" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"></path></svg>
  </a>

  <Footer calLink={calLink} waLink={waLink} />
</BaseLayout>

<style>
  .marquee-track { animation: marquee 34s linear infinite; }
  @keyframes marquee { to { transform: translateX(-50%); } }
  .cases-track { display: flex; gap: 1.5rem; overflow-x: auto; scroll-snap-type: x mandatory; scrollbar-width: none; }
  .cases-track::-webkit-scrollbar { display: none; }
  .case-card { scroll-snap-align: start; flex: 0 0 88%; max-width: 540px; }
  @media (min-width: 640px) { .case-card { flex: 0 0 48%; } }
  @media (min-width: 1024px) { .case-card { flex: 0 0 31.5%; } }
  .dot-active { background-color: var(--color-ember); }
  @media (max-width: 640px) { .marquee-track { animation-duration: 24s; } }
</style>
